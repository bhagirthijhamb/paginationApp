import Filter from "./Filter";

const Filters = (props) => {
  return (
    <>
      <Filter placeholder="Search by name" />
      <Filter placeholder="Search by tag" />
    </>
  );
};

export default Filters;
